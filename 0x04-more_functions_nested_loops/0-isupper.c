#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: Variant text
 * Return: Always (0)
 */
int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
	{
		return (1);
	else
		return (0);
	}
}
